{
  "name": "contenthub-integration",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@chili-publish/studio-sdk": "^0.148.0"
  },
  "scripts": {
    "build": "bun run helpers/pre_build.js && bun build --target=browser --outfile=public/js/sdk.js sdk-frontend.ts",
    "build-studio": "bun run helpers/pre_build.js && bun build --target=browser --outfile=public/js/studio.js studio-frontend.ts",
    "server": "bun run build && bun run --watch server.ts"
  }
}