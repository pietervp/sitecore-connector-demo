{
  "name": "studio-ui",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "bun build --target=browser --outfile=public/js/index.js frontend.ts",
    "server": "bun run --watch server.ts"
  }
}